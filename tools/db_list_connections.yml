type: function
name: db_list_connections
description: |
  List configured database connections with driver type, current availability status, and a sanitized failure reason when probing fails.
inputSchema:
  type: object
  additionalProperties: false
