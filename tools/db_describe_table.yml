type: function
name: db_describe_table
label: 数据表结构详情
description: |
  Describe columns, primary key, and indexes for one table.
inputSchema:
  type: object
  properties:
    connection_name:
      type: string
      description: Configured connection name.
    schema:
      type: string
      description: Schema name. Defaults to the connection's current default schema/database.
    table:
      type: string
      description: Table name.
  required:
    - connection_name
    - table
  additionalProperties: false
