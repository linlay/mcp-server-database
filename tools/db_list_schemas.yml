type: function
name: db_list_schemas
description: |
  List schemas or logical databases visible to the selected connection.
inputSchema:
  type: object
  properties:
    connection_name:
      type: string
      description: Configured connection name.
  required:
    - connection_name
  additionalProperties: false
